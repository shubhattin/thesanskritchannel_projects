{
  "name": "Container",
  "initializeCommand": "echo 'Skipping default npm install'",
  "postCreateCommand": "git config --global pull.rebase false && npm i -g pnpm tsx && pnpm install && mkdir .venv && pip install uv && uv sync"
}
