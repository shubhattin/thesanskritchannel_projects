{
  "name": "redirect",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/bun-serve.ts",
    "build": "vite build",
    "start": "cd dist && bun index.js",
    "start:node": "cd dist && node index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}