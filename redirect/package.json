{
  "name": "redirect",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "vite build",
    "start": "bun run dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}