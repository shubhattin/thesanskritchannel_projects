# Prettier formats ONLY .astro files — oxfmt (see .oxfmtrc.json) handles svelte/ts/js/json/css.
#
# LEGACY: prettier is kept ONLY because oxfmt does not support Astro yet.
# When oxfmt gains native Astro support, delete this file + prettier-plugin-astro,
# remove "**/*.astro" from .oxfmtrc.json ignorePatterns, and let oxfmt format everything.
# See README.md > "Formatting & Linting".

# ignore everything...
*
# ...but keep traversing directories...
!*/
# ...and format only .astro files
!*.astro

# vendored, never reformat
src/components/Posthog.astro
